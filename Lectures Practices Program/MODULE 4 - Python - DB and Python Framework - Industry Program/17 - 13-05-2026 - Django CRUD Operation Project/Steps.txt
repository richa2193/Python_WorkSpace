===============================
DJANGO CRUD OPERATION PROJECT
===============================

1. settings.py change from sqlite to mysql database and project name - newdbapp

Open XAMPP and start:
- Apache
- MySQL

Open phpMyAdmin and create database named:

newdbapp


========================================
2. MAKE MIGRATE (CREATE DEFAULT TABLES)
========================================

Run command:

python manage.py migrate

This command will create default Django tables
(around 10 tables).


=====================
3. CREATE APP
=====================

Run command:

python manage.py startapp crudapp


===================================
4. REGISTER APP IN settings.py
===================================

Open:

settings.py

Add app name inside INSTALLED_APPS:

'crudapp',


=========================================
5. CREATE templates FOLDER AND urls.py
=========================================

Inside crudapp create:

templates folder

and create file:

crudapp/urls.py


=================================================
6. INCLUDE MAIN URL IN newdbapp/urls.py
=================================================

Open:

newdbapp/urls.py

Copy this code:

from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    path('', include('crudapp.urls')),
]

Now open:

crudapp/urls.py

Import views:

from django.urls import path
from . import views


=========================================
7. CREATE HTML PAGES USING CHATGPT
=========================================

Prompt:

In my Django app CRUD operation app required some html pages.

Design index.html page for data display in table format.

Design one more html page for data insert with fields:
- name
- email
- mobile
- dob

Design one more html page for data updation.

Design all html pages responsive using Tailwind CSS with:
- black and white theme
- gradients
- modern UI


===================================
8. CREATE index.html
===================================

Features:
- Display data in table format
- Add Edit button
- Add Delete button
- Add Add Data button


===================================
9. CREATE insert.html
===================================

Create form fields:
- Name
- Email
- Mobile
- DOB

Add submit button.


===================================
10. CREATE update.html
===================================

Same form as insert.html

But old data should already be filled inside form fields.


===================================
11. CREATE 3 VIEWS IN views.py
===================================

Views:
- index
- insert
- update


===================================
12. CREATE 3 URLS IN urls.py
===================================

Example:

urlpatterns = [
    path('', views.index),
    path('insert/', views.insert),
    path('update/<int:id>/', views.update),
]


===================================
13. RUNSERVER TO CHECK INDEX PAGE
===================================

Run command:

python manage.py runserver

Open browser:

http://127.0.0.1:8000/

Now change all a href links properly.


===================================
14. ADD DATA BUTTON IN index.html
===================================

Example:

<a href="/insert/">Add Data</a>


==================================================
BACKEND START
==================================================


===================================
15. CREATE MODEL IN models.py
===================================

Open:

crudapp/models.py

Create class for database table:

from django.db import models

class Student(models.Model):
    name = models.CharField(max_length=100)
    email = models.EmailField()
    mobile = models.CharField(max_length=15)
    dob = models.DateField()


=====================================================
16. RUN COMMAND FOR CREATE TABLE (MIGRATIONS)
=====================================================

Run commands:

python manage.py makemigrations
python manage.py migrate


=====================================================
17. CHECK METHOD POST AND NAME FIELD
=====================================================

In insert.html form:

- Add method="POST"
- Add csrf token
- Add name attribute in all fields

Example:

<form method="POST">

{% csrf_token %}

<input type="text" name="name">

Button click submit.

Request first goes from HTML form
to views.py
then forms.py
then database.


===================================
18. CREATE forms.py FILE
===================================

Create file:

crudapp/forms.py

Import model and create ModelForm:

from django.forms import ModelForm
from .models import Student

class StudentForm(ModelForm):

    class Meta:
        model = Student
        fields = '__all__'


=====================================================
19. INSERT CODE IN views.py
=====================================================

First import:

from django.shortcuts import render, redirect
from .forms import StudentForm
from .models import Student

Now in insert view:

- Check request method POST
- Receive form data
- Save data in database

Example logic:

if request.method == "POST":

    form = StudentForm(request.POST)

    if form.is_valid():
        form.save()

        return redirect('/')


===================================
20. RUNSERVER AGAIN
===================================

Run:

python manage.py runserver

Now insert data and check database.


=====================================================
21. DISPLAY DATA IN INDEX PAGE
=====================================================

In views.py index function:

Fetch all data:

data = Student.objects.all()

Pass data in context:

return render(request,'index.html',{'data':data})

Now display data in table format in index.html.


===================================
22. INSERT AND ADD DONE
===================================

Now Add Data button working successfully.

Data insert operation completed.


=====================================================
23. ADD EDIT LINK IN index.html
=====================================================

Example:

<a href="/update/{{i.id}}">Edit</a>


=====================================================
24. GET DATA USING ID FOR UPDATE
=====================================================

In urls.py:

path('update/<int:id>/', views.update)

In views.py:

def update(request,id):

Get record:

data = Student.objects.get(id=id)

Pass that variable in context.

In update.html:

Add value field:

value="{{data.name}}"

value="{{data.email}}"

value="{{data.mobile}}"

value="{{data.dob}}"


=====================================================
25. UPDATE EXISTING RECORD
=====================================================

Copy insert form code.

Use:

instance=stid

Example:

form = StudentForm(request.POST,instance=stid)

Save updated data.


=====================================================
26. CREATE DELETE VIEW
=====================================================

Same process like update.

Create delete view in views.py

Pass id from index.html


=====================================================
27. ADD DELETE LINK
=====================================================

In index.html:

<a href="/delete/{{i.id}}">Delete</a>

In urls.py:

path('delete/<int:id>/', views.delete)


=====================================================
28. DELETE RECORD USING ID
=====================================================

In views.py:

def delete(request,id):

    data = Student.objects.get(id=id)

    data.delete()

    return redirect('/')


===================================
FINAL COMMANDS
===================================

Install mysqlclient:

pip install mysqlclient


Create migrations:

python manage.py makemigrations


Apply migrations:

python manage.py migrate


Run server:

python manage.py runserver


===================================
PROJECT COMPLETE
===================================